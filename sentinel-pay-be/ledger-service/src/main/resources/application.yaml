spring:
  application:
    name: ledger-service

  datasource:
    url: jdbc:mysql://localhost:3306/sentinelpay
    username: root
    password: suhail

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true




server:
  port: 8086
