server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sentinelpay
    username: root
    password: suhail
    driver-class-name: com.mysql.cj.jdbc.Driver
  kafka:
    bootstrap-servers: localhost:9092

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true